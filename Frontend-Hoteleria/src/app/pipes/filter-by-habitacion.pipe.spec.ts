import { FilterByHabitacionPipe } from './filter-by-habitacion.pipe';

describe('FilterByHabitacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByHabitacionPipe();
    expect(pipe).toBeTruthy();
  });
});
